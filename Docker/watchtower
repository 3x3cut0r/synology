### Watchtower ###

### Watchtower ###
# https://hub.docker.com/r/containrrr/watchtower

docker container run -d --restart=always \
    --name=watchtower \
    -v /var/run/docker.sock:/var/run/docker.sock \
    -v /etc/timezone:/etc/timezone:ro \
    containrrr/watchtower:1.0.3
